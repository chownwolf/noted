services:
  noted:
    image: node:20-alpine
    working_dir: /app
    network_mode: host
    volumes:
      - ./:/app
      - ./notes:/app/notes
    command: sh -c "npm install && npm start"
    restart: unless-stopped
